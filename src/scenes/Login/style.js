import React from 'react';
import { StyleSheet } from 'react-native';

export default Styles = StyleSheet.create({
    button: {
        color: 'blue',
        fontWeight: 'bold',
        fontSize: 30,
    },
    label: {
        color: 'red',
    },
    error: {
        color: 'green',
    },
});