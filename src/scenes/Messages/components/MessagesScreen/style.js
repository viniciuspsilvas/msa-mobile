import React, { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        margin: 10,
    },

    title: {
        margin: 10,
        fontSize: 19,
        fontWeight: 'bold',
    }
})